<script type="text/javascript">

function getDescriptionWidth()
{
  var leftWidthPercent = 0;
  var horizontalFrame = parent.document.getElementById("horizontalFrame");
  if (horizontalFrame != null) {
    leftWidthPercent = horizontalFrame.cols.split("%")[0];
  }
  var mainWidthMax = screen.availWidth*(100-leftWidthPercent)/100;
  return (mainWidthMax*55/100);
}

</script>

<table style="margin:0px auto; width:600px; border-spacing:2px;">
  <tr>
    <td style="vertical-align:top;">
    <% unless @item.summary.nil? or @item.summary.empty? %>
      <div style="padding-bottom:10px;">
        <%= render(:partial => 'show_summary') %>
      </div>
    <% end %>
      <div>
        <%= render(:partial => 'show_description') %>
      </div>
    </td>
    <td style="vertical-align:top;">
      <table style="width:100%;">
        <tr>
          <td style="vertical-align:top; width:30%;">
            <% if @item.attachments_without_content.length > 0 %>
             <%= render(:partial => 'show_attachment') %>
            <% end %>
          </td>
        </tr>
        <tr style="height:10px;"><td></td></tr>
        <tr>
          <td>
            <% if @item.images_without_content.length > 0 %>
              <%= render(:partial => 'show_image') %>
            <% end %>
          </td>
        </tr>
      </table>
    </td>
  </tr>
</table>


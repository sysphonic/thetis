
<% login_user = session[:login_user] %>

<table align="center" width="100%" cellpadding="0" cellspacing="1">
  <tr>
    <td align="left">
      <% 
        path = session[:text_path]
        debugger # Y.BAN
        f = open(path)
        f.each do |textline|
      %>
          <%= textline %>
      <%
        end
      %>
    </td>
  </tr>
</table>

<%= render(:partial => 'common/flash_notice') %>
